<%= form_for(@now_assign) do |f| %>
  <% if @now_assign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@now_assign.errors.count, "error") %> prohibited this now_assign from being saved:</h2>

      <ul>
      <% @now_assign.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.text_field :student_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
