<%= form_for(@past_assign) do |f| %>
  <% if @past_assign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@past_assign.errors.count, "error") %> prohibited this past_assign from being saved:</h2>

      <ul>
      <% @past_assign.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.text_field :student_id %>
  </div>
  <div class="field">
    <%= f.label :year %><br>
    <%= f.number_field :year %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
